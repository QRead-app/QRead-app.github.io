---
    const { name, email, role } = Astro.props;
---

<div class="font-bold border-2 rounded-xl px-3 py-1">
    <div class="text-xl text-primary italic">{name}</div>
    
    <div class="mt-2 font-light">
        <div>{email}</div>
        <div>{role}</div>
    </div>
</div>